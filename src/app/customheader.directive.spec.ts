import { CustomheaderDirective } from './customheader.directive';

describe('CustomheaderDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomheaderDirective();
    expect(directive).toBeTruthy();
  });
});
